export enum EcEntityDomain {
    User,
    School,
    SelfPeer,
    Cognitive
}

export class EcEntityType {
    static Person = 'Person';
    static Campus = 'Campus';
    static CourseSection = 'CourseSection';
}

export enum EcContextType {
    User,
    School,
    SelfPeer,
    Cont
}
